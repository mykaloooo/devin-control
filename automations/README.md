# Automations 导出

导出时间：2026-06-22（源账号）

**结果：源账号当前没有任何 Devin Automation（`devin_automation_manage list` 返回 0 条）。**

因此本目录为空占位，新账号无需重建任何 Automation。

> 历史：`kaka-media/2026IP` 仓里曾有 GitHub Actions 工作流 `auto-sync-mykaloooo.yml`（自动合并 Devin PR + 镜像到老仓）。**单仓化后镜像部分已停用**，不再需要 `MYKALOOOO_MIRROR_TOKEN`。如保留「自动合并 Devin PR」，用仓库自带 `secrets.GITHUB_TOKEN` 即可，随代码走，无需在 Devin 后台重建。
